// The global variable
let fixedValue = 4;

// Only change code below this line
function incrementer(val) { // Pure function (do not mutate args)
  return val + 1;

  // Only change code above this line
}